apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: {{ k8s_namespace }}
spec:
  type: {{ nginx_service_type | default('NodePort') }}
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: metrics
      port: 9113
      targetPort: 9113
