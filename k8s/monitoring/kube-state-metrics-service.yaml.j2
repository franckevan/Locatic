apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
  namespace: {{ k8s_namespace }}
spec:
  type: ClusterIP
  selector:
    app: kube-state-metrics
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080